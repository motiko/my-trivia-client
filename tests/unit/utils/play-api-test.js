import playApi from 'trivia/utils/play-api';
import { module, test } from 'qunit';

module('Unit | Utility | play api');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = playApi();
  assert.ok(result);
});
